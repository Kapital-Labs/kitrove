# Synthetic Harness Fixtures

Fixtures model harness homes without copying real user data. Each fixture should identify the observed harness version and the evidence used to construct it.

Required fixture classes include empty state, valid portable skill, supporting resources, malformed metadata, duplicate identity, co-owned configuration, native executable content, path traversal attempts, symlink escape attempts, authentication canaries, and unknown future versions.
